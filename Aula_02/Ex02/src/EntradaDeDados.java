import java.util.Scanner;

public class EntradaDeDados {
	/*
	 * Exemplos de entrada de dados 
	 * para os tipos principais.
	 */
	
	int numInt = new Scanner(System.in).nextInt();
	double numFl = new Scanner(System.in).nextDouble();
	char varChar = new Scanner(System.in).next().charAt(0);
	String varStr = new Scanner(System.in).nextLine();
	boolean varBoo = new Scanner(System.in).nextBoolean();
}
